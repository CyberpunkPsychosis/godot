# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository State

This repository is currently a near-empty starter: it contains only a stub `README.md` and this file. There is no source code, build system, test suite, or CI configuration yet.

Despite the name, this is **not** a fork of the Godot game engine source tree — do not assume Godot engine conventions (SCons build, `modules/` architecture, etc.) apply unless engine code is actually added.

## Guidance for Working Here

- Before documenting or referencing commands (build, lint, test), verify they exist — nothing is set up yet, so any tooling must be established first as part of the task at hand.
- When code is first added, update this file with the real build/test commands and the architecture that emerges, replacing this placeholder guidance.
